import * as account from './account.js'
import * as invests from './invests.js'
import * as projects from './projects.js'

const actions = Object.assign({}, account, invests , projects);

export default actions
